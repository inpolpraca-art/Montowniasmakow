MONTOWNIA SMAKÓW — READY PACKAGE

VIDEO: 04_ASSETS/07_SHOWREEL.mp4 is included in this package. It is the uploaded videoplayback.mp4 renamed for the site. The player is local MP4, muted, autoplay and starts from 27 seconds.

DAILY RECOMMENDATION: the 7-day selector was removed. The page automatically detects the visitor's weekday and shows the recommendation for that day, with a different restaurant photo.

LIGHTHOUSE ROBOTS.TXT: if Lighthouse is opened directly from file://, its robots.txt audit can fail because robots.txt is fetched over a supported web protocol. Test the deployed HTTPS site (or an HTTP server) for the production result.
